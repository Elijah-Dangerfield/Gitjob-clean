package com.dangerfield.gitjob.domain.usecases.search

import junit.framework.TestCase

/*
- test when cache fails that error is returned
- test when cache success that success is returned
- test when city is empty that cache returns
 */
class AddSearchedTermTest : TestCase()